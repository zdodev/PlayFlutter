name: json_placeholder_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  
  # State Management
  flutter_bloc: ^9.1.1

  # Routing
  auto_route: ^11.1.0

  # Dependency Injection
  get_it: ^9.2.0
  injectable: ^2.7.1+4

  freezed_annotation: ^3.1.0
  json_annotation: ^4.10.0

  dio: ^5.9.1
  retrofit: ^4.9.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

  auto_route_generator: ^10.4.0
  build_runner: ^2.10.5
  freezed: ^3.2.4
  json_serializable: ^6.12.0
  injectable_generator: ^2.12.0
  retrofit_generator: ^10.2.1

flutter:
  uses-material-design: true